<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.syariahrooms">
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.MyMaterialTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".permintaankhusus"></activity>
        <activity android:name=".ReservationActivity" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.home.LupaPasswordActivity" />
        <activity android:name=".articleActivity" />
        <activity android:name=".login.Login" />
        <activity android:name=".ui.home.register" />
        <activity android:name=".HotelDetailActivity" />
        <activity android:name=".ui.home.FilterHotelActivity" />
        <activity android:name=".RoomDetailActivity" />
        <activity android:name=".RoomListActivity" />
        <activity android:name=".ui.hotel_list.activity.HotelListActivity" />
        <activity android:name=".LocationListActivity" />
        <activity android:name=".ui.home.MainActivity">

        </activity>
        <!-- tambahan -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyADnIbWTC_K7LF25jbdNaZcC-BrlI1ETEk" />

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
        <!-- tambahan -->
    </application>

</manifest>